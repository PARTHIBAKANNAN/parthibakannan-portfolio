# Copy this file to ".dev.vars" for local testing of the chatbot function
# with:  npm run build  &&  npx wrangler pages dev dist
#
# .dev.vars is gitignored — never commit your real key.
GEMINI_API_KEY=your-gemini-api-key-here
